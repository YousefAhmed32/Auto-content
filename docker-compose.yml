services:
  yansy-publish:
    build: .
    restart: unless-stopped
    env_file:
      - server/.env
    ports:
      - "4000:4000"
    volumes:
      - ./server/data:/app/server/data
      - ./server/uploads:/app/server/uploads

